# This is a basic workflow to help you get started with Actions

name: Deliver on host

# Controls when the action will run. 
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  release:
    types: [published]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2
      - name: GitHub GraphQL API Query
        # You may pin to the exact commit or the version.
        # uses: octokit/graphql-action@5b35793a23d626e7528da63fc3b47a5bc06b628c
        uses: octokit/graphql-action@v2.2.1
        id: get_latest_release
        with:
          query: |
                query release($owner:String!,$repo:String!) {
                  repository(owner:$owner, name:$repo) {
                    latestRelease {
                      description
                    }
                  }
                }
          owner: ${{ github.repository_owner }}
          repo: ${{ github.event.repository.name }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Send chat to group
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USER }}
          password: ${{ secrets.PASSWORD }}
          port: 22
          script: |
            curl -v --data-urlencode "group_id=$GROUPNO" --data-urlencode "message=$MSG" "http://127.0.0.1:5700/send_group_msg"
          script_stop: true
          envs: GROUPNO,MSG 
        env:
          GROUPNO: ${{ secrets.NOTIFY_GROUP_ID }}
          MSG: ${{ format('damebot is updating... \n {0}', fromJSON(steps.get_latest_release.outputs.data).repository.releases.nodes) }}
#       # Runs a single command using the runners shell
#       - name: ssh-scp-ssh-pipelines
#         # You may pin to the exact commit or the version.
#         # uses: cross-the-world/ssh-scp-ssh-pipelines@e1c2bf0c8d7813a3243dc649b6af178262a834e9
#         uses: cross-the-world/ssh-scp-ssh-pipelines@v1.1.2
#         with:
#           # ssh remote host
#           host: ${{ secrets.HOST }}
#           # ssh remote user
#           user: ${{ secrets.USER }}
#           # ssh remote password
#           pass: ${{ secrets.PASSWORD }}
#           first_ssh: |
#             rm -rf /home/${{ secrets.USER }}/app-damebot
#             mkdir /home/${{ secrets.USER }}/app-damebot
#           # scp from local to remote
#           scp: |
#             . => /home/${{ secrets.USER }}/app-damebot
#           # execute post-commands after scp
#           last_ssh: |
#             docker-compose up -d --build
      - name: copy file via ssh key
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USER }}
          password: ${{ secrets.PASSWORD }}
          port: 22
          source: "."
          target: "/home/${{ secrets.USER }}/app-damebot"
          rm: true
      - name: Start up damebot service
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USER }}
          password: ${{ secrets.PASSWORD }}
          port: 22
          script: |
            cd app-damebot
            docker-compose up -d --build
          script_stop: true
      - name: Send chat to group
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USER }}
          password: ${{ secrets.PASSWORD }}
          port: 22
          script: |
               curl -v --data-urlencode "group_id=$GROUPNO" --data-urlencode "message=$MSG" "http://127.0.0.1:5700/send_group_msg"
          script_stop: true
          envs: GROUPNO,MSG
        env:
          GROUPNO: ${{ secrets.NOTIFY_GROUP_ID }}
          MSG: ${{ format('damebot update complete. \n {0}', fromJSON(steps.get_latest_release.outputs.data).repository.releases.nodes) }}
  
            
